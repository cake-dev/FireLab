optimization problem
	whats the minimal change to the features that would change the 	classifcation from high to low burn severity
	examine weather conditions that contribute towards a burn severity

proj location:
export PROJ_LIB=/home/jake/.conda/envs/gisenv/lib/python3.11/site-packages/rasterio/proj_data/proj.db


ERROR 1: PROJ: internal_proj_identify: Cannot find proj.db


UPDATE: first run worked, check2 has the parquet data w/mtbs, geom, ig date, and dem info
UPDATE: next run worked, mtbs_df_temp_path has the mtbs data all above + daymet data
FIXME: adding biomass returns error of no data in raster (need to fix projection)

Geohash for unique id at each pixel or incrementing integer